/**
 * Shared dimmed overlay behind modals, sheets, and confirm dialogs.
 */
export const MODAL_BACKDROP_CLASS_NAME = 'bg-ink/70 backdrop-blur-[2px]';

/**
 * Bottom-sheet backdrop — no blur (blur + unmount causes a full-page paint flash).
 */
export const SHEET_BACKDROP_CLASS_NAME = 'bg-ink/70';
