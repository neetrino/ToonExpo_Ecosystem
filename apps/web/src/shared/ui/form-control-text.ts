/**
 * 16px below `lg` so iOS Safari does not zoom on focus.
 * Pair with the unlayered rule in `base.css` (catches raw fields + `text-sm` overrides).
 */
export const FORM_CONTROL_TEXT_CLASS = 'text-base lg:text-sm';
