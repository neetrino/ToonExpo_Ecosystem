/** Public project detail route — same convention as catalog cards and expo booth links. */
export const buildProjectPublicHref = (projectId: string): `/projects/${string}` =>
  `/projects/${projectId}`;
