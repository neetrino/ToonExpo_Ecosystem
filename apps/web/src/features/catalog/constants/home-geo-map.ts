/** Home page 3D map panel sizing — matches prior map banner proportions. */
export const HOME_GEO_MAP_HEIGHT_CLASS = 'min-h-80 h-[28rem] lg:min-h-[42rem] lg:h-[42rem]';

/** Max autocomplete results shown in the home map project search. */
export const HOME_GEO_MAP_SEARCH_MAX_RESULTS = 8;
