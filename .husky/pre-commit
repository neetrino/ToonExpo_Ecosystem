#!/usr/bin/env sh

# Cursor / GUI git often has a minimal PATH without nvm/pnpm.
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
if [ -s "$NVM_DIR/nvm.sh" ]; then
  # shellcheck disable=SC1091
  . "$NVM_DIR/nvm.sh"
fi

pnpm exec lint-staged
